DIMENSION TEST(7)
DO 100 I=1,7
    READ*,TEST(I)
100 CONTINUE
ISMALL=TEST(1)
DO 200 I=2,7
    IF(TEST(I).LT.ISMALL) ISMALL=TEST(I)
200 CONTINUE
SUM=0.0
DO 300 I=1,7
    IF(TEST(I).EQ.ISMALL) GO TO 300
    SUM=SUM+TEST(I)
300 CONTINUE
AVE=SUM/6.0
PRINT*,'THE FINAL GRADE IS ',AVE
STOP
END
